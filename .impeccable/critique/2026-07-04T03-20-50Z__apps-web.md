---
target: apps/web (all screens)
total_score: 23
p0_count: 0
p1_count: 3
timestamp: 2026-07-04T03-20-50Z
slug: apps-web
---
Method: dual-agent (A: design-review sub-agent · B: detector/browser sub-agent)

# Design Health Score — 23/40 (Acceptable band; honest for a mock-data prototype)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Run-tests result renders as 11.5px corner text at the loop's peak moment |
| 2 | Match System / Real World | 3 | "faults", "par", "fluency 88" never defined in-product |
| 3 | User Control and Freedom | 2 | No Esc anywhere; no abandon; no way back after submit; refresh kills the rep silently |
| 4 | Consistency and Standards | 3 | ⌘R shadows browser reload; Practice tab silently deep-links to warmup's active slug |
| 5 | Error Prevention | 1 | Global ⌘Enter submits from any focus state, irreversibly; no beforeunload guard |
| 6 | Recognition Rather Than Recall | 3 | Shortcuts printed under buttons; PAR always adjacent to elapsed |
| 7 | Flexibility and Efficiency | 2 | Two shortcuts total; no /-search, no next-rep key, no prompt-toggle key |
| 8 | Aesthetic and Minimalist Design | 4 | Dense but earned; hairline discipline genuinely good |
| 9 | Error Recovery | 1 | Fail/timeout paths don't exist; debrief fabricates a PASS for unattempted reps |
| 10 | Help and Documentation | 1 | No onboarding, no formula disclosure, no tooltips |
| **Total** | | **23/40** | |

# Anti-Patterns Verdict

**Does not read as AI-made.** Deterministic scan: 0 findings (detect.mjs clean); zero hex colors, zero backdrop-blur, zero gradient text, zero uppercase-class kickers; !important only inside the reduced-motion kill-switch. LLM review agrees the "athletic timing instrument" identity landed structurally (splits, pars, fault odometer, rep dots, PR stamp, cobalt heatmap) — not just mono digits sprinkled on dark SaaS.

Two borderline items both assessments converged on:
- `LandingScreen.tsx:50-51` "FLUENCY TRAINING FOR ENGINEERS" + THE REP/THE CLOCK/THE DECAY labels — the banned eyebrow-kicker pattern implemented via literal caps + tracking-[…]; defensible inside the product as instrument labels, a director-circle on the brand-register landing.
- `WarmupScreen.tsx:91` full-width cobalt top stripe — nearest relative of the banned side-stripe.
- `DebriefScreen.tsx:70` `z-10` bypasses the semantic z-scale used correctly elsewhere.

No browser overlays were injected (read-only evaluation); console clean across all routes.

# What's Working

1. **Time as material, verified live** — every numeral routes through `.mono` (tabular-nums + tnum) and the stopwatch formatters in `lib/format.ts`; timer ticks without digit jitter.
2. **Honesty mechanics are real** — paste probe: defaultPrevented=true, FAULTS 00→01, text never entered the doc; modifier keys excluded from keystroke counts; autocomplete absent by construction.
3. **Token discipline of unusual quality** — three-tier borders, documented micro-shades, semantic z-scale, OKLCH-only, focus-ring offset parameterized per surface (`ButtonOffset`).

# Priority Issues

1. **[P1] The failure path doesn't exist and the debrief fabricates passes.** `DebriefScreen.tsx:29-51` synthesizes verdict "pass" (fluency 82, "A clean rep.") for ANY slug, including never-attempted ones. VerdictPill fail/timeout exist only in /design-system. The honesty product structurally cannot be honest. Fix: wire real verdicts (arrives with Sprint 3 session state), design the FAIL/TIMEOUT debrief, sessionless visits show "no rep recorded". → /impeccable harden (with Sprint 3)
2. **[P1] Global ⌘Enter submits the rep from any focus, instantly, irreversibly.** `PracticeScreen.tsx:61-74` binds window; no confirm, no recovery (state is component-local). Fix: scope to editor focus or confirm-beat; persist session state. → fix pre-Sprint-3
3. **[P1] The label layer fails WCAG AA.** Computed from tokens: muted-2 on bg 4.24:1 / on surface 4.08:1 at 10.5–11px (ELAPSED, FAULTS, kbd hints, table headers); faint 2.77:1 (heatmap axes, status text); syn-com 3.4:1. Fix now — tokens propagate: raise muted-2 → L≈0.62, faint → L≈0.53 for informational text. → /impeccable audit + token edit
4. **[P2] No responsive floor.** Deterministic at 375px: /library scrollWidth 702 (nav gap-7 = 414px global; filter pills 445px), /log 519 (heatmap grid 792px), /warmup 520, /debrief 642 (hard `w-[400px]` sidebar). At ~1100px the debrief verdict band overlaps and diff panes crush to slivers. Desktop-first is the brief, but 1280-with-sidebar is desktop. → /impeccable adapt
5. **[P2] Run-tests feedback is the weakest UI at the strongest moment.** `5/5 · 11ms` in faint corner text after a fake 550ms; runState never returns to idle. The session rail is the instrument panel — results belong there as splits. → with S3-3

# Persona Red Flags

**Alex (power user):** ⌘R shadows reload; zero Escape listeners in the codebase; no /-focus search; "Rebuild session" and "Skip today" are dead buttons (no onClick) — trust dents.
**Sam (a11y):** Tab is a keyboard trap in the editor (indentWithTab, CM6 Esc-then-Tab convention unadvertised); global `:focus-visible { outline: none }` means unclassed elements get NO indicator; editor focus visible only as caret; SegmentedControl role="radio" without arrow-key handling; LAST_7 today-cell and Sparkline direction are color-alone; heatmap SR-labeled only as a total.
**Riley (stress tester):** invalid slug → clean NotFound ✓; direct debrief → fabricated pass ✗; refresh mid-rep → all state lost, no warning; library header claims "128 challenges · 42 trained" over 13 real rows — dishonest numbers in the honesty product.

# Minor Observations

- `lib/motion.ts` (revealUp/splitList/prStamp) is imported by nothing — designed motion vocabulary is dead code; CSS `cr-split` is the parallel implementation that shipped. One source of truth should win.
- Reduced-motion handling is genuinely good (default-visible splits via `both` fill mode).
- `promptOpen` reads window.innerWidth once at mount; no resize response.
- Landing footer links are inert spans at 2.77:1 — look broken at brand register.
- Debrief hand-rolls its pass treatment instead of using VerdictPill — the component built for exactly this.
- Timer starts on route mount, before first keystroke — defensible, make it explicit.

# Questions to Consider

1. Can the timer be peripheral if time is the material — should elapsed ghost into the editor gutter where the eyes are?
2. The FAIL debrief is the product — evening practitioners fail often; that screen decides retention. Why does the sad path have zero design?
3. Is the streak flame athletic or Duolingo? The Log's LAST_7 strip may already be the honest version.
4. "88/100" with no disclosable formula is exactly what the honesty principle bans.
